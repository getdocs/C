#include<stdio.h>
#include<stdbool.h>

void main() {
/*
* sizeof is function which is used for checking size of datatype
* it takes the input of variable name and output the size in integer
* syntax of sizeof is:
* sizeof(datatype)
*/
int int_size = sizeof(int);
int float_size = sizeof(float);
int char_size = sizeof(char);
int long_long_size = sizeof(long long);
int double_size = sizeof(double);
int long_double_size = sizeof(long double);
int bool_size = sizeof(bool);
}
