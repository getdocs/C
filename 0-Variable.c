#include<stdio.h>

void main() {
/*
* variables are used for storing:
* simple data, user input, mathematical expressions result
* they are temporary storage block
* once program execution ends,
* all the data inside them deleted
* different types of data can be stored in variables
* some datatypes are integer, decimal and character
* variables declaration mean define the datatype
* after defining the dataype, variable can be initialized
* syntax of variable declaration
* dataype_name variable_name;
* syntax of variable initialization
* variable_name = value;
* both steps can be done together
* datatype_name variable_name = value;
* variable name can be any text with general criteria
* variable must be alphanumeric with only one special character '_'
* variable name cannot be started with number
*/
}
